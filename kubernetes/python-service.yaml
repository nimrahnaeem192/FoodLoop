apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-service
  namespace: foodloop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: python-service
  template:
    metadata:
      labels:
        app: python-service
    spec:
      containers:
        - name: python-service
          image: foodloop/python-service:latest
          ports:
            - containerPort: 8000
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: python-service
  namespace: foodloop
spec:
  selector:
    app: python-service
  ports:
    - port: 8000
      targetPort: 8000
